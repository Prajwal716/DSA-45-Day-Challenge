1. Containers With Most Water:

Code:
class Solution {
    public int maxArea(int[] height) {
        int left = 0;
        int right = height.length - 1;

        int maxWater = 0;

        while (left < right) {

            int width = right - left;
            int currentHeight = Math.min(height[left], height[right]);

            int currentArea = width * currentHeight;

            maxWater = Math.max(maxWater, currentArea);

            // Move the pointer with the smaller height
            if (height[left] < height[right]) {
                left++;
            } else {
                right--;
            }
        }

        return maxWater;
    }
}

Approach:
Use the two-pointer technique with pointers at both ends. Calculate the area using the smaller height, update the maximum area, then move the pointer with the smaller height inward to potentially find a larger area.

Complexity:
Time: O(n) — each pointer moves toward the other at most once.
Space: O(1) — only a few variables are used.

------------------------------------------------------------------------------------------------------------------------------------------------------------------

2. Trapping Rain Water:

Code:
class Solution {
    public int trap(int[] height) {
        int left = 0;
        int right = height.length - 1;

        int leftMax = 0;
        int rightMax = 0;

        int water = 0;

        while (left < right) {

            if (height[left] <= height[right]) {

                if (height[left] >= leftMax) {
                    leftMax = height[left];
                } else {
                    water += leftMax - height[left];
                }

                left++;

            } else {

                if (height[right] >= rightMax) {
                    rightMax = height[right];
                } else {
                    water += rightMax - height[right];
                }

                right--;
            }
        }

        return water;
    }
}

Approach

Use the two-pointer technique while maintaining leftMax and rightMax, the maximum heights seen from both sides. Process the side with the smaller height because the trapped water there is determined by that side's maximum boundary.

Complexity
Time: O(n) — each element is processed at most once.
Space: O(1) — only pointers and maximum-height variables are used.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------

3. Two Sum II - Input Array Is Sorted:

Code:
class Solution {
    public int[] twoSum(int[] numbers, int target) {
        int left = 0;
        int right = numbers.length - 1;

        while (left < right) {
            int sum = numbers[left] + numbers[right];

            if (sum == target) {
                return new int[]{left + 1, right + 1};
            } 
            else if (sum < target) {
                left++;
            } 
            else {
                right--;
            }
        }

        return new int[]{-1, -1};
    }
}

Approach:
Use the two-pointer technique since the array is sorted. Start from both ends; if the sum is smaller than the target, move left forward, otherwise move right backward until the target sum is found.

Complexity:
Time: O(n) — each pointer moves at most n times.
Space: O(1) — only constant extra variables are used.
